[pytest]
testpaths = unit integration
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --strict-markers

markers =
    unit: Tests unitaires
    integration: Tests d'intégration
    critical: Tests critiques (VTOR, etc.)
    vtor: Tests VTOR
    startup: Tests startup
